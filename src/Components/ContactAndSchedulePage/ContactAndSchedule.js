// Import required modules
import React, { useState } from 'react'

// Create a React functional component (ContactForm) using React Hooks
const ContactForm = () => {
  return (
		<div className="Page-container">Contact and Schedule page</div>
	)
}
export default ContactForm;

// Import required modules
import React, { useState } from 'react'

// Create a React functional component (HomePage) using React Hooks
const HomePage = () => {
  return (
		<div className="Page-container">Home page</div>
	)
}
export default HomePage;
